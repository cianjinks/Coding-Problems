//https://leetcode.com/explore/featured/card/top-interview-questions-easy/92/array/770/

#include <vector>
#include <iostream>
#include <algorithm>

class Solution {
public:
    void rotate(std::vector<std::vector<int>>& matrix) {
        // Lets try just rotating the outer ring
        int n = (int)matrix[0].size();
        for(int x = 0; x < n; x++)
        {
            for(int y = 0; y < n; y++)
            {
                
            }
        }
    }
};

int main()
{
    return 0;
}